<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--Nap szám -->
        <Label Content="Nap szám" Grid.Row="0" Grid.Column="0" Margin="3" VerticalAlignment="Center"/>
        <ComboBox x:Name="txtNapSzam" Grid.Row="0" Grid.Column="1" Margin="3,6,3,6"/>



        <!--Elkészitett -->
        <Label Content="elkeszitett" Grid.Row="0" Grid.Column="2" Margin="3" VerticalAlignment="Center"/>
        <TextBox x:Name="txtElkeszitett" Grid.Row="0" Grid.Column="3" Margin="3,6,3,6"/>
        
        <!-- Eladott -->
        <Label Content="eladott" Grid.Row="0" Grid.Column="4" Margin="3" VerticalAlignment="Center"/>
        <TextBox x:Name="txtEladott" Grid.Row="0" Grid.Column="5" Margin="3,6,3,6"/>

        <Button Content="hozzaad" Grid.Column="6" Grid.Row="0" Margin="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="hozzaadas"/>

        <ListBox x:Name="lstAdatok" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="7" Margin="20"/>
    </Grid>
</Window>
